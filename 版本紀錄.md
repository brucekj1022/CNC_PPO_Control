# CNC使用PPO 版本紀錄

> **State 定義**: `s(t) = [action(t), Path_FFT(t+1), maxResonance(t-1), sumError(t-1)]` → 產出 `action(t+1)`  
> **Action 限制**: a = 14

---

## v2026.4.24
> 上一版本: 2026.1.13

**訓練規則**
| Learning Rate | Episodes |
|---------------|----------|
| 1e-5 | 3000 |
| 1e-6 | 6000 |
| 1e-7 | 6000 |

**摘要**
- 許多細節優化
- 定期保存功能

**更改**
- `CNC.py`: 共振壓制流程更動，以前 QCQP 無解不會進行延遲倒數，現在不管 QCQP 有沒有解都會倒數

---

## v2026.1.13
> 上一版本: 2025.12.22

**摘要**
- 解決 `plot_exp_data` 畫系統性能指標 PM 會出現負角度問題（官方函數的鍋）

**更改**
- `Plot_return`: 擴充成能畫出 Model 資料夾裡面所有訓練狀況，或可選單一筆資料畫
- `CNC.py`: 計算 Reward 的 `def FCfreq(FC, Wgc)` 計算方式更動

---

## v2026.1.5
> 上一版本: 2025.12.22

**摘要**
- 更新一些畫圖功能

---

## v2025.12.22
> 上一版本: 2025.12.10

**摘要**
- 合併 `plot_exp_data` 與 `plot_multi_exp_data`
- 更改 `plot_exp_data` 的畫圖樣式

---

## v2025.12.10
> 上一版本: 2025.11.24

**摘要**
- `ran` / `unc` 訓練時用不同權重，主要改 `w_sumError`（ran: 1e+2, unc: 1e+3）
- Runtime 新增檔案使用 Switch 模式: `Runtime_SwitchModel`
- `Runtime_SwitchModel` 原本是 model1 semisolve 才換 model，現在檢查到共振直接切換

**新增**
- `Runtime_SwitchModel`

**更改**
- `Training`: 訓練參數
- `Runtime`: 顯示每步資料

---

## v2025.11.24
> 上一版本: 2025.9.16

**摘要**
- 傳給 LabVIEW 的 CC 係數不用反轉了
- 重新訓練模型，讓參考路徑能吃到 10Hz，訓練路徑振幅從 10 調到 1

**更改**
- `Runtime`:
  - `fft_limit_freq = 30` (path_FFT 取到幾 Hz)
- `CNC.py` → `class PathModel`:
  - 刪除 reference path
  - 把 test path 變為兩個：0~1Hz / 0~20Hz
  - Training path 開放到 20Hz

---

## v2025.9.16 (完整版)
> 上一版本: 2025.8.18

**訓練規則**
| Learning Rate | Episodes |
|---------------|----------|
| 1e-5 | 1000 |
| 1e-6 | 2000 |
| 1e-7 | 3000 |

**摘要**
- 參考路徑引入超低頻以及有 DC 偏移，在路徑轉 FFT 之前先減去均值（濾掉 DC）
- 這版本 ID 新的 Model，機台所有 velocity IO 資料全部換成新的
- 重新產生給 Python 用的 delta，更改 `CNC` 裡的 `ID_Plant` 參數
- 解決因限制點增益太靠近 1 而造成的數值問題
- 參考限制點第四點原本設在 Wgc 上，改成 Wgc 跟最大頻率中間均勻分布

**刪除**
- `Training`: `path_min_freq` 跟 `path_max_freq` 兩個變數
- 舊 delta 檔案
- `Runtime_sharememory`
- `Cost_test`

**新增**
- 新 delta 檔案
- `Runtime` 用 TCP 跟 cRIO 交流

**更改**
- `Training`:
  - `def path_FFT`: 把全域變數 `path_min_freq` 換成區域變數 `min_freq`，並在轉換傅立葉前先把均值減掉
  - 有關 `ek_buffer` 時序問題，取消模擬延遲，`ek` 直接寫入 `data_buffer`
- `CNC.py`:
  - `class PathModel`: 有關生路徑的變數都刪掉
  - `class CNCModel` → `def ID_Plant`: 平台建模參數
  - `class Costfunction`:
    - `use_central_controller`: 第四個限制點不要直接放在中央 OLoop Wgc 上
    - `Frequence_domain_coefficient`: 新增查 FC（只避開 ki=1；大於 1 往下 -1e-8，否則往上 +1e-8）
  - `class PlotExporter` → `def plot_error`: 限制劃出的範圍在 ±300

---

## v2025.8.18
> 上一版本: 2025.7.27

**摘要**
- 上個版本有兩種 Model，這版本試試 switch model
- ⚠️ 這個版本訓練時會有 inf 數值問題產生（已解決）

**刪除**
- `CNC.py` → `class PathModel` → `def step_path()`
- `LabVIEWBridge` 資料夾

**新增**
- `CNC.py` → `class PathModel` → `def noise_path()`

**更改**
- `SwitchModel`: 關於 model 名稱跟一些設定
- `Runtime` 更名為 `Runtime_sharememory`

---

## v2025.7.27
> 上一版本: 2025.6.12

**摘要**
- 調整隨機共振出現機率，讓低頻共振峰值不要太誇張大
- 這版經過訓練後 400 rad/s 的共振也壓得下去，犧牲 Wgc

**備註**
> 在 Model 裡面：
> - `random`: 用 random plant 訓練，壓制共振效果很好
> - `unc`: 用 uncertainty plant 訓練不加隨機共振，追跡效果很好

**更改**
- `CNC.py` → `class CNCModel` → `def random_Plant`

---

## v2025.6.12
> 上一版本: 2025.4.14

**摘要**
- Model 部份加上 uncertainty，uncertainty 資料由 MATLAB 提供
- 已訓練完 uncertainty plant + with cost Wgc / without cost Wgc / ID plant 三個組合
- ⚠️ 老師覺得 Wgc 變動範圍不夠大，在 400 rad/s 的共振壓不下來，認為神經網路應該要能自己判別降低 Wgc

**更改**
- `CNC.py`:
  - `uncertainty_Plant` 更名為 `test_Plant`
  - `uncertainty_Plant` 改用來創造高頻不確定性
- `Training`:
  - `uncertainty_Plant` 更名為 `test_Plant`

---

## v2025.4.14
> 上一版本: 2025.4.7

**摘要**
- 上版本分別使用有 costWgc 跟沒有 costWgc 進行訓練
  - 有 costWgc 的 model：遇到無解共振時會自主降低 Wgc，但追跡效能較差
  - 單純靠 cost error 訓練的 model：追跡效能很好，但抗共震能力差
- 這版嘗試結合使用：一般使用追跡好的 model，當共振 3 step 都無法降低 FC 就切換成穩定的 model
- 隨機共振改一下權重，低頻率出現的機會增高
- 檔案更名: `Runtime` → `Training`
- 創建用於 LabVIEW function node 的讀取書寫 share memory C 語言檔案
- 資料大整理，路徑重新分配

**刪除**
- `Training` 裡存 model 的地方: `'resonanceTable': costfunction_x.resonanceTable`

**新增**
- `Runtime`: 用來上機測試
- `SwitchModel`: 用模擬切換 Model 效能
- `CNC.py` → `class PathModel` → `def output_path`

**更改**
- `Runtime` 檔名改為 `Training`
